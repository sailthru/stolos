- make missing job_id identifiers just assume we depend on all of them.
  - dag_tools tests for this
- make job_id_validations file optional.  raise error if it's not a python path
  - consider util.load_... use a filepath instead of a python path?

Lock and LockingQueue

majorityredis - errors due to connection or server errors cannot be mixed with
expectable errors returned from server?  dunno.
  -  qbcli.create may cause confusion here.
